extends layout

block content
  h1= title
  p Welcome to #{title}
  each message in messages 
    div 
      h2= message.text
      p= message.user
      p= message.added
  a(href="/new") Add new message
